#!/usr/bin/env bash
set -eo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ "${1:-}" == "setup" ]]; then
  python3 "${ROOT}/tools/dev.py" "$@"
  exec python3 "${ROOT}/tools/qgroundcontrol.py" setup
fi

exec python3 "${ROOT}/tools/dev.py" "$@"
